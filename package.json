{
  "name": "Roblox cookie Manager",
  "version": "1.0.1",
  "description": "Just an extension to make login with cookies more easy.",
  "main": "index.js",
  "type": "module",
  "private": false,
  "engines": {
    "node": ">= 14.0.0",
    "npm": ">= 6.0.0"
  },
  "homepage": "",
  "repository": {
    "type": "git",
    "url": ""
  },
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "npx tsc",
    "copy-assets": "rsync -av --exclude 'tsconfig.json' --exclude 'src' --exclude 'dist' ./ dist/",
    "build": "npm run clean && npm run compile && npm run copy-assets"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.267", 
    "typescript": "^5.4.5"
  }
}
